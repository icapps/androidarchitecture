def buildConfig = [
        buildToolsVersion: '27.0.0',
        compileSdkVersion: 27,
        targetSdkVersion : 27,
        minSdkVersion    : 16,
]

ext.config = [
        "buildConfig": buildConfig
]