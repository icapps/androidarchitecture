def buildConfig = [
        buildToolsVersion: '29.0.2',
        compileSdkVersion: 29,
        targetSdkVersion : 29,
        minSdkVersion    : 16,
]

ext.config = [
        "buildConfig": buildConfig
]